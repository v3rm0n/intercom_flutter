name: regular_integration_tests
publish_to: none

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  intercom_flutter_web:
    path: ../
  flutter_driver:
    sdk: flutter
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

environment:
  sdk: ">=3.0.0 <4.0.0"
